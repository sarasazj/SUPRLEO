// Optional: paste your N2YO API key between the quotes to enable live ISS pass data.
// The dashboard also checks localStorage key "suprN2yoApiKey" before falling back to cached/demo pass estimates.
window.SUPR_N2YO_API_KEY = "";

// Backward compatibility for older local demos that used the UPR variable name.
window.UPR_N2YO_API_KEY = window.SUPR_N2YO_API_KEY;
